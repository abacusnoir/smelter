#!/usr/bin/env smt run

(declare add-two (Integer -> Integer))
(define (add-two x)
  (+ x 2))

(declare test-equality (Integer -> Integer -> Boolean))
(define (test-equality x y)
  (== x y))

(declare main Unit)
(define main unit)